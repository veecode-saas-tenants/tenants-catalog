apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: platform-devportal-components
  description: Importing components
spec:
  targets:
    - ./demo-trivy/*.yaml
    - ./demo-viacep/*.yaml
