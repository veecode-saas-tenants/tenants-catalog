#
# WORKFLOW PARA EDICAO E TESTES DE SKAFFOLDING
#
services:
  backstage:
    #image: andrevtg/devportal-bundle:magalu
    image: veecode/devportal-bundle:0.23.2
    ports:
      - 3000:3000
      - 7007:7007
    environment:
      - NODE_ENV=development
    volumes:
      - ./.compose/app-config.minimal.yaml:/app/app-config.minimal.yaml
      - ./templates:/opt/templates
      #- /tmp:/tmp
    command: ["node", "packages/backend", "--config", "app-config.minimal.yaml"]
